---
import Layout from "@/layouts/Layout.astro";
import Container from "@/components/ui/Container.astro";
import { SITE_NAME } from "@/lib/constants";
---

<Layout title={`Terms of Service — ${SITE_NAME}`}>
  <Container class="flex flex-col gap-6 py-24">
    <h1 class="text-3xl font-semibold tracking-tight">Terms of Service</h1>
    <p class="max-w-2xl text-muted-foreground">
      Placeholder — full terms of service content is coming before launch.
      This page exists so the link in the footer resolves to something real
      rather than a dead end.
    </p>
  </Container>
</Layout>
